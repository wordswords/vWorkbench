#!/bin/bash


if [ "$(id -u)" -ne 0 ] ; then
    echo 'You must be root to run this script'
    exit 1
fi

set -x
set -e

/bin/nohup /bin/screen -S Mirror-Erista -dm bash -c "/home/david/.dotfiles/bin/mirror-erista.sh $1" 2>&1 > /home/hqconfig/.mirror_erista.log

